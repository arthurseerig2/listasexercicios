#include <stdio.h>

int main () {
	float base, altura, area;
	printf("valor da base: \n"); 
	printf("valor da altura: \n");

	area = (base * altura) / 2;

	printf("o valor da area eh: %f\n", area); scanf("%f.2%f", area); // resolver o scanf
  
}
